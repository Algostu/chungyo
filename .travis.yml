language: python
python:
  - "3.6.4"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: 
  - pytest pose_diff/interface/
  - pytest pose_diff/core/
  - pytest pose_diff/DB/
  - pytest pose_diff/util/
